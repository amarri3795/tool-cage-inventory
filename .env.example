# Copy to .env and fill in your Neon connection string.
# Use the Neon "pooled" connection string for Vercel (port 5432, often includes -pooler).

DATABASE_URL="postgresql://USER:PASSWORD@HOST/neondb?sslmode=require"
